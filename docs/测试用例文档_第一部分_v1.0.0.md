# 测试用例文档 v1.0.0

> **文档版本**：v1.0.0
> **日期**：2026-06-21
> **依据文档**：
>
> - 任务分解\_WBS_v1.1.0.md
> - 旅游攻略生成平台\_SRS v1.1.0.md
> - 前端交互设计规格书 v1.0.9.md
> - API接口设计规格书 v1.0.6.md
> - 数据库设计规格书 v1.0.3.md
> - Trip_Lifecycle引擎算法设计 v1.0.0.md
>   **说明**：每个任务按 4 个维度编写测试用例：功能验收 / 边界验收 / 单元测试 / 集成测试

---

## 阅读说明

| 标识    | 含义                       |
| ------- | -------------------------- |
| ✅ 通过 | 测试用例通过               |
| ❌ 失败 | 测试用例失败，需修复       |
| ⏭ 阻塞 | 依赖任务未完成，暂无法执行 |
| 🔄 重跑 | 修复后需重新执行           |

---

## 1. 阶段一：基础设施层测试用例

---

### 1.1 INFRA-001 Monorepo 初始化

#### 功能验收测试用例

| 用例ID         | 用例描述              | 前置条件            | 操作步骤                                 | 预期结果                                                |
| -------------- | --------------------- | ------------------- | ---------------------------------------- | ------------------------------------------------------- |
| INFRA-001-F-01 | 目录结构正确创建      | 无                  | 执行 `turbo init`，按 SRS 9.1 节创建目录 | `apps/web`、`apps/mobile`、`packages/shared` 等目录存在 |
| INFRA-001-F-02 | package.json 生效     | 目录已创建          | 根目录执行 `npm install`                 | 依赖安装成功，无 ERESOLVE 错误                          |
| INFRA-001-F-03 | Tsconfig 基础配置生效 | package.json 已生效 | 编译 `packages/shared` 下 TS 文件        | 编译通过，输出到 `dist/`                                |
| INFRA-001-F-04 | Turbo 管道配置正确    | Tsconfig 已生效     | 执行 `turbo run build`                   | 按依赖顺序构建，`apps/web` 最后构建                     |

#### 边界验收测试用例

| 用例ID         | 用例描述                       | 前置条件         | 操作步骤               | 预期结果                                        |
| -------------- | ------------------------------ | ---------------- | ---------------------- | ----------------------------------------------- |
| INFRA-001-B-01 | Node 版本不匹配时给出提示      | 本地 Node < 18   | 执行 `turbo run build` | 命令行输出「Node 版本必须 ≥18，当前为 X」并退出 |
| INFRA-001-B-02 | 磁盘空间不足时安装失败给出提示 | 磁盘剩余 < 500MB | 执行 `npm install`     | 输出「磁盘空间不足，需要至少 500MB」并退出      |
| INFRA-001-B-03 | 网络不通时依赖安装失败提示     | 断网             | 执行 `npm install`     | 输出「网络不可用，请检查代理设置」并退出        |

#### 单元测试用例

```typescript
// packages/shared/tests/monorepo.test.ts

describe("Monorepo 结构验证", () => {
  it("应存在 turbo.json 配置文件", () => {
    const filePath = path.resolve(__dirname, "../../../turbo.json");
    expect(fs.existsSync(filePath)).toBe(true);
  });

  it("turbo.json 中应声明 build/dev/lint 三个 pipeline", () => {
    const config = JSON.parse(fs.readFileSync("turbo.json", "utf-8"));
    expect(config.pipeline).toHaveProperty("build");
    expect(config.pipeline).toHaveProperty("dev");
    expect(config.pipeline).toHaveProperty("lint");
  });

  it("packages/shared 的 package.json 应包含 main 字段指向 dist/", () => {
    const pkg = JSON.parse(
      fs.readFileSync("packages/shared/package.json", "utf-8"),
    );
    expect(pkg.main).toContain("dist/");
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                        | 操作步骤                                                    | 预期结果                                 |
| -------------- | ------------------------------- | ----------------------------------------------------------- | ---------------------------------------- |
| INFRA-001-I-01 | 从零初始化到首次构建通过        | 1. 克隆仓库 2. `npm install` 3. `turbo run build`           | 所有 package 和 app 构建成功，无错误日志 |
| INFRA-001-I-02 | 修改 shared 后 web 自动重新构建 | 1. 修改 `packages/shared/src/types.ts` 2. `turbo run build` | `apps/web` 检测到 shared 变更，重新构建  |

---

### 1.2 INFRA-002 PostgreSQL + Prisma

#### 功能验收测试用例

| 用例ID         | 用例描述                       | 前置条件          | 操作步骤                             | 预期结果                                                |
| -------------- | ------------------------------ | ----------------- | ------------------------------------ | ------------------------------------------------------- |
| INFRA-002-F-01 | Docker Compose 启动 PostgreSQL | Docker 已安装     | `docker-compose up -d postgres`      | `pg_isready` 返回成功                                   |
| INFRA-002-F-02 | Prisma Schema 首次迁移         | PostgreSQL 已启动 | `npx prisma migrate dev --name init` | `prisma/migrations/` 生成 SQL 文件，数据库中表已创建    |
| INFRA-002-F-03 | Prisma Client 生成成功         | 迁移已完成        | `npx prisma generate`                | `@prisma/client` 生成成功，可 `import { PrismaClient }` |
| INFRA-002-F-04 | Prisma Studio 可访问           | 迁移已完成        | `npx prisma studio`                  | 浏览器打开 `http://localhost:5555`，可查看表数据        |

#### 边界验收测试用例

| 用例ID         | 用例描述                            | 前置条件                           | 操作步骤                  | 预期结果                                               |
| -------------- | ----------------------------------- | ---------------------------------- | ------------------------- | ------------------------------------------------------ |
| INFRA-002-B-01 | PostgreSQL 未启动时 Prisma 连接报错 | PostgreSQL 停止                    | 执行 `npx prisma db pull` | 报错「Can't reach database server」，不导致进程崩溃    |
| INFRA-002-B-02 | 数据库连接池满时新连接排队          | 已建立 10 个连接（默认 pool 上限） | 发起第 11 个查询          | 查询排队等待，超时（30s）后报错                        |
| INFRA-002-B-03 | Schema 有破坏性变更时 migrate 提示  | Schema 删除已有字段                | `npx prisma migrate dev`  | 提示「This migration may cause data loss」，需手动确认 |

#### 单元测试用例

```typescript
// packages/backend/tests/prisma.test.ts

describe("Prisma 连接测试", () => {
  const prisma = new PrismaClient();

  afterAll(async () => {
    await prisma.$disconnect();
  });

  it("应能成功连接到 PostgreSQL", async () => {
    const result = await prisma.$queryRaw<{ ok: boolean }>`SELECT 1 as ok`;
    expect(result[0].ok).toBe(true);
  });

  it("users 表应支持写入和读取", async () => {
    const user = await prisma.user.create({
      data: {
        email: "test@example.com",
        passwordHash: "hash",
        nickname: "测试用户",
      },
    });
    expect(user.email).toBe("test@example.com");
    // 清理
    await prisma.user.delete({ where: { id: user.id } });
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述               | 操作步骤                                                                         | 预期结果                           |
| -------------- | ---------------------- | -------------------------------------------------------------------------------- | ---------------------------------- |
| INFRA-002-I-01 | 完整迁移→生成→启动流程 | 1. `prisma migrate deploy` 2. `prisma generate` 3. 启动后端服务                  | 后端服务启动成功，/health 返回 200 |
| INFRA-002-I-02 | 数据库重置后重新迁移   | 1. `docker-compose down -v` 2. `docker-compose up -d` 3. `prisma migrate deploy` | 所有表重新创建，无数据残留         |

---

### 1.3 INFRA-003 Redis + BullMQ

#### 功能验收测试用例

| 用例ID         | 用例描述                  | 前置条件        | 操作步骤                                      | 预期结果                                                     |
| -------------- | ------------------------- | --------------- | --------------------------------------------- | ------------------------------------------------------------ |
| INFRA-003-F-01 | Docker Compose 启动 Redis | Docker 已安装   | `docker-compose up -d redis`                  | `redis-cli ping` 返回 `PONG`                                 |
| INFRA-003-F-02 | BullMQ 创建队列成功       | Redis 已启动    | 执行 `Queue('test').add('job1', { data: 1 })` | Redis 中写入 key `bull:test:*`                               |
| INFRA-003-F-03 | Worker 消费队列任务       | 队列中有任务    | 启动 Worker                                   | Worker 日志输出「Processing job1」，任务状态变为 `completed` |
| INFRA-003-F-04 | 失败任务进入重试          | Worker 抛出异常 | 添加任务，Worker 抛错                         | 任务最多重试 3 次（默认），之后进入 `failed` 状态            |

#### 边界验收测试用例

| 用例ID         | 用例描述                   | 前置条件                   | 操作步骤                      | 预期结果                                    |
| -------------- | -------------------------- | -------------------------- | ----------------------------- | ------------------------------------------- |
| INFRA-003-B-01 | Redis 内存满时写入失败     | Redis `maxmemory` 设为 1MB | 添加大任务（>1MB）            | 抛出 `MaxMemoryError`，不影响已有任务       |
| INFRA-003-B-02 | Redis 未启动时 BullMQ 报错 | Redis 停止                 | 执行 `Queue('test').add(...)` | 抛出 `ECONNREFUSED`，有重试逻辑（指数退避） |
| INFRA-003-B-03 | 任务数据超过 512MB 时报错  | —                          | 添加 513MB 的 JSON 任务       | 抛出 `BULLMQ_TASK_TOO_LARGE`，拒绝添加      |

#### 单元测试用例

```typescript
// packages/backend/tests/redis-bullmq.test.ts

describe("BullMQ 队列测试", () => {
  const queue = new Queue("test-queue", { connection: redisConnection });

  afterAll(async () => {
    await queue.close();
  });

  it("应成功添加任务到队列", async () => {
    const job = await queue.add("test-job", { tripId: "123" });
    expect(job.id).toBeDefined();
    expect(job.data.tripId).toBe("123");
  });

  it("任务应在 30 秒内被 Worker 消费", async () => {
    const job = await queue.add("delayed-job", { test: true });
    const result = await job.waitUntilFinished(30000);
    expect(result).toBeDefined();
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                       | 操作步骤                                      | 预期结果                        |
| -------------- | ------------------------------ | --------------------------------------------- | ------------------------------- |
| INFRA-003-I-01 | Redis 宕机恢复后队列任务不丢失 | 1. 添加 10 个任务 2. 停止 Redis 3. 重启 Redis | 任务仍在队列中，Worker 继续消费 |
| INFRA-003-I-02 | 多个 Worker 并发消费同一队列   | 启动 3 个 Worker 进程，添加 20 个任务         | 20 个任务被均匀分配，无重复消费 |

---

### 1.4 INFRA-004 高德 API 适配层

#### 功能验收测试用例

| 用例ID         | 用例描述                   | 前置条件          | 操作步骤                                              | 预期结果                                           |
| -------------- | -------------------------- | ----------------- | ----------------------------------------------------- | -------------------------------------------------- |
| INFRA-004-F-01 | 景点搜索返回正确格式       | 高德 API Key 有效 | 调用 `searchPOI('西湖', '杭州')`                      | 返回 `AMapPOI[]`，含 `name`/`location`/`address`   |
| INFRA-004-F-02 | 路线规划返回正确格式       | 高德 API Key 有效 | 调用 `planRoute('杭州西湖', '杭州灵隐寺', 'driving')` | 返回 `AMapRoute`，含 `distance`/`duration`/`steps` |
| INFRA-004-F-03 | 天气查询返回正确格式       | 高德 API Key 有效 | 调用 `getWeather('杭州')`                             | 返回 `AMapWeather`，含 `weather`/`temperature`     |
| INFRA-004-F-04 | API Key 无效时返回明确错误 | 使用无效 Key      | 调用 `searchPOI('西湖', '杭州')`                      | 抛出 `AMapAPIError`，`code=40003`，不崩溃          |

#### 边界验收测试用例

| 用例ID         | 用例描述                         | 前置条件                 | 操作步骤                                        | 预期结果                                 |
| -------------- | -------------------------------- | ------------------------ | ----------------------------------------------- | ---------------------------------------- |
| INFRA-004-B-01 | 每秒超过 10 次请求时触发限流保护 | 高德免费版 Key（QPS=10） | 并发 20 个请求                                  | 超出部分进入队列等待，不触发高德限流错误 |
| INFRA-004-B-02 | 网络超时（>5000ms）时重试        | 模拟网络延迟 6000ms      | 调用 `searchPOI`                                | 自动重试 3 次，全部失败后抛出超时错误    |
| INFRA-004-B-03 | 城市名不存在时返回空结果         | —                        | 调用 `searchPOI('xxx不存在的景点', '未知城市')` | 返回空数组 `[]`，不抛异常                |

#### 单元测试用例

```typescript
// packages/backend/tests/amap-client.test.ts

describe("高德 API 适配层", () => {
  it("应将高德返回的 POI 映射为内部 Poi 类型", async () => {
    const result = await amapClient.searchPOI("西湖", "杭州");
    expect(result[0]).toMatchObject({
      name: expect.any(String),
      latitude: expect.any(Number),
      longitude: expect.any(Number),
    });
  });

  it("高德返回错误码时应抛出 AMapAPIError", async () => {
    amapClient.setTestKey("INVALID_KEY");
    await expect(amapClient.searchPOI("西湖", "杭州")).rejects.toThrow(
      AMapAPIError,
    );
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                        | 操作步骤                                                   | 预期结果                                                                                 |
| -------------- | ------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| INFRA-004-I-01 | 高德 API 不可用时降级到静态数据 | 1. 断开高德 API 网络 2. 调用 `searchPOI`                   | 返回静态景点数据（ cities.json 中预设），并在日志中记录「高德 API 不可用，使用静态数据」 |
| INFRA-004-I-02 | 高德API限流后自动切换备选Key    | 1. 主 Key 触发限流（返回 `10003`） 2. 再次调用 `searchPOI` | 自动切换备选 Key，请求成功                                                               |

---

### 1.5 INFRA-005 LLM 多提供商调用层

#### 功能验收测试用例

| 用例ID         | 用例描述                  | 前置条件                   | 操作步骤                                                | 预期结果                                 |
| -------------- | ------------------------- | -------------------------- | ------------------------------------------------------- | ---------------------------------------- |
| INFRA-005-F-01 | DeepSeek 调用成功返回文本 | DeepSeek API Key 有效      | 调用 `llm.call('deepseek', prompt)`                     | 返回 `{ content: string, usage: {...} }` |
| INFRA-005-F-02 | GLM 调用成功返回文本      | GLM API Key 有效           | 调用 `llm.call('glm', prompt)`                          | 返回同上格式                             |
| INFRA-005-F-03 | 提供商优先级切换          | DeepSeek 和 GLM Key 均有效 | 配置 `priority: ['deepseek', 'glm']`，DeepSeek 调用失败 | 自动切换 GLM，返回结果                   |
| INFRA-005-F-04 | 流式调用返回 SSE chunk    | DeepSeek API Key 有效      | 调用 `llm.callStream('deepseek', prompt)`               | 返回 `AsyncGenerator<string>`            |

#### 边界验收测试用例

| 用例ID         | 用例描述                   | 前置条件                                | 操作步骤                                | 预期结果                                                    |
| -------------- | -------------------------- | --------------------------------------- | --------------------------------------- | ----------------------------------------------------------- |
| INFRA-005-B-01 | 所有提供商均失败时抛出错误 | 所有 Key 无效                           | 调用 `llm.callWithFallback(prompt)`     | 抛出 `AllLLMProvidersFailedError`，包含每个提供商的失败原因 |
| INFRA-005-B-02 | Token 超限时自动截断输入   | 输入 token 数 > 模型 max_tokens         | 调用 `llm.call('deepseek', longPrompt)` | 输入被截断到最大允许长度，并记录警告日志                    |
| INFRA-005-B-03 | 并发请求超过限制时排队     | 同一提供商并发 > 限制（如 DeepSeek 10） | 并发 20 个请求                          | 超出部分进入队列，不触发提供商限流错误                      |

#### 单元测试用例

```typescript
// packages/backend/tests/llm-provider.test.ts

describe("LLM 多提供商调用层", () => {
  it("应按优先级依次尝试提供商", async () => {
    const result = await llmService.callWithFallback("test prompt", {
      priority: ["deepseek", "glm", "xiaomi"],
      timeoutPerProvider: 5000,
    });
    expect(result.provider).toBeDefined();
  });

  it("所有提供商失败时抛出聚合错误", async () => {
    mockAllProvidersFail();
    await expect(llmService.callWithFallback("test")).rejects.toThrow(
      AllLLMProvidersFailedError,
    );
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                               | 操作步骤                          | 预期结果              |
| -------------- | -------------------------------------- | --------------------------------- | --------------------- | ---------------------------------- |
| INFRA-005-I-01 | 完整降级链路：DeepSeek→GLM→Xiaomi→Kimi | 依次使 DeepSeek、GLM、Xiaomi 失败 | Kimi 最终成功返回结果 | 总耗时 < 3 倍单提供商超时时间      |
| INFRA-005-I-02 | LLM 返回格式异常时自动修正             | Mock LLM 返回非法 JSON            | 调用引擎解析 LLM 返回 | 触发重试或返回格式错误提示，不崩溃 |

---

### 1.6 INFRA-006 SSE 流式框架

#### 功能验收测试用例

| 用例ID         | 用例描述                  | 前置条件       | 操作步骤                              | 预期结果                                                   |
| -------------- | ------------------------- | -------------- | ------------------------------------- | ---------------------------------------------------------- |
| INFRA-006-F-01 | SSE 连接建立成功          | 后端服务运行中 | 前端 `EventSource('/trips/generate')` | `onopen` 触发，`readyState=1`                              |
| INFRA-006-F-02 | 后端发送 `connected` 事件 | SSE 连接已建立 | 后端 `sendSSE(event, data)`           | 前端 `onmessage` 收到 `{ type: 'connected', data: {...} }` |
| INFRA-006-F-03 | 后端发送 `progress` 事件  | SSE 连接已建立 | 后端发送多次 `progress`               | 前端进度条平滑更新，无闪烁                                 |
| INFRA-006-F-04 | 连接正常关闭              | 生成完成       | 后端 `res.end()`                      | 前端 `onclose` 触发                                        |

#### 边界验收测试用例

| 用例ID         | 用例描述                       | 前置条件                   | 操作步骤                | 预期结果                                 |
| -------------- | ------------------------------ | -------------------------- | ----------------------- | ---------------------------------------- |
| INFRA-006-B-01 | 客户端主动断开时服务端清理资源 | 前端 `EventSource.close()` | 监听 `close` 事件       | 服务端取消正在进行的 LLM 调用，释放资源  |
| INFRA-006-B-02 | 网络中断后重连                 | 生成过程中断网 10 秒后恢复 | 前端自动重连            | 重连成功，服务端从断点继续发送事件       |
| INFRA-006-B-03 | SSE 超时（>300s）时自动关闭    | —                          | 保持连接 301 秒无新事件 | 服务端主动关闭连接，前端提示「生成超时」 |

#### 单元测试用例

```typescript
// packages/backend/tests/sse.test.ts

describe("SSE 流式框架", () => {
  it("应正确格式化 SSE 事件", () => {
    const formatted = formatSSEEvent("progress", { percent: 50 });
    expect(formatted).toBe('event: progress\ndata: {"percent":50}\n\n');
  });

  it("应处理事件数据中的特殊字符", () => {
    const formatted = formatSSEEvent("message", { text: "hello\nworld" });
    expect(formatted).toContain("hello\\nworld");
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                                             | 操作步骤                          | 预期结果                                   |
| -------------- | ---------------------------------------------------- | --------------------------------- | ------------------------------------------ |
| INFRA-006-I-01 | 完整 SSE 流程：connected→progress×N→day_ready×N→done | 调用生成接口                      | 前端按正确顺序收到所有事件，状态机正确流转 |
| INFRA-006-I-02 | 多客户端同时连接同一生成任务                         | 2 个浏览器标签同时打开同一 tripId | 两个标签均收到完整 SSE 事件（广播模式）    |

---

### 1.7 INFRA-007 JWT 鉴权中间件

#### 功能验收测试用例

| 用例ID         | 用例描述            | 前置条件               | 操作步骤                                             | 预期结果                                  |
| -------------- | ------------------- | ---------------------- | ---------------------------------------------------- | ----------------------------------------- |
| INFRA-007-F-01 | 有效 JWT 可通过鉴权 | 已登录，拿到有效 token | 请求 Header 带 `Authorization: Bearer <token>`       | 接口返回 200，req.user 包含 userId        |
| INFRA-007-F-02 | 无效 JWT 返回 401   | —                      | 请求 Header 带 `Authorization: Bearer invalid_token` | 接口返回 401，`{ error: 'InvalidToken' }` |
| INFRA-007-F-03 | 无 token 返回 401   | —                      | 请求不带 Authorization Header                        | 接口返回 401，`{ error: 'MissingToken' }` |
| INFRA-007-F-04 | 过期 JWT 返回 401   | token 已过期           | 请求带过期 token                                     | 接口返回 401，`{ error: 'TokenExpired' }` |

#### 边界验收测试用例

| 用例ID         | 用例描述                        | 前置条件                         | 操作步骤     | 预期结果                                              |
| -------------- | ------------------------------- | -------------------------------- | ------------ | ----------------------------------------------------- |
| INFRA-007-B-01 | token 在过期前 10 秒内刷新      | token 还剩 5 秒过期              | 请求到达后端 | 后端返回新 token（ `x-refresh-token` Header）         |
| INFRA-007-B-02 | 非法格式的 Authorization Header | Header 值为 `Bearer`（无 token） | 发送请求     | 返回 401，`{ error: 'MalformedAuthorizationHeader' }` |
| INFRA-007-B-03 | 并发请求使用同一 token          | 2 个请求同时使用同一有效 token   | 发送请求     | 两个请求均通过鉴权（token 无状态，支持并发）          |

#### 单元测试用例

```typescript
// packages/backend/tests/jwt-auth.test.ts

describe("JWT 鉴权中间件", () => {
  it("应正确签发 JWT", () => {
    const token = jwtService.sign({
      userId: "user-123",
      email: "test@example.com",
    });
    expect(token).toBeDefined();
    expect(typeof token).toBe("string");
  });

  it("应正确验证有效 JWT", () => {
    const token = jwtService.sign({ userId: "user-123" });
    const payload = jwtService.verify(token);
    expect(payload.userId).toBe("user-123");
  });

  it("过期 JWT 应抛出 TokenExpiredError", () => {
    const token = jwtService.sign({ userId: "user-123" }, { expiresIn: "-1s" });
    expect(() => jwtService.verify(token)).toThrow("TokenExpired");
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                                  | 操作步骤                                                                     | 预期结果                                |
| -------------- | ----------------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------- |
| INFRA-007-I-01 | 登录→拿到 token→访问受保护接口→刷新 token | 1. POST /auth/login 2. 使用返回 token 访问 GET /trips 3. 使用过期 token 访问 | 步骤 2 成功；步骤 3 返回 401 及新 token |
| INFRA-007-I-02 | 注销后 token 立即失效                     | 1. 登录拿到 token 2. POST /auth/logout 3. 使用同一 token 访问受保护接口      | 步骤 3 返回 401                         |

---

### 1.8 INFRA-008 API 限流中间件

#### 功能验收测试用例

| 用例ID         | 用例描述                   | 前置条件 | 操作步骤                           | 预期结果                                           |
| -------------- | -------------------------- | -------- | ---------------------------------- | -------------------------------------------------- |
| INFRA-008-F-01 | 正常请求不被限流           | —        | 发送 < 100 次/分钟 的请求          | 所有请求返回 200                                   |
| INFRA-008-F-02 | 超过限流阈值返回 429       | —        | 同一 IP 发送 101 次请求/分钟       | 第 101 次及之后返回 429，`Retry-After` Header 有值 |
| INFRA-008-F-03 | 不同 IP 互不影响           | —        | IP-A 发送 100 次，IP-B 发送 100 次 | 两者均不被限流                                     |
| INFRA-008-F-04 | 限流计数器在时间窗口后重置 | —        | 限流触发后等待 61 秒，再发送请求   | 请求成功（计数器已重置）                           |

#### 边界验收测试用例

| 用例ID         | 用例描述                         | 前置条件                    | 操作步骤       | 预期结果                                                   |
| -------------- | -------------------------------- | --------------------------- | -------------- | ---------------------------------------------------------- |
| INFRA-008-B-01 | Redis 不可用时限流降级为内存模式 | Redis 停止                  | 发送请求       | 限流仍在内存中生效，日志输出「Redis 不可用，使用内存限流」 |
| INFRA-008-B-02 | 同一用户多 IP 场景               | 同一用户从 3 个 IP 发送请求 | 总请求数 > 100 | 按 IP 分别限流（每个 IP 独立计数）                         |
| INFRA-008-B-03 | 超大计数（>2^31）时不溢出        | Mock Redis 返回超大计数     | 发送请求       | 计数器重置为 0，记录错误日志                               |

#### 单元测试用例

```typescript
// packages/backend/tests/rate-limit.test.ts

describe("API 限流中间件", () => {
  it("应在超过阈值时返回 429", async () => {
    const agent = request(app);
    // 发送 100 次请求
    for (let i = 0; i < 100; i++) {
      await agent.get("/api/trips");
    }
    // 第 101 次
    const res = await agent.get("/api/trips");
    expect(res.status).toBe(429);
    expect(res.headers["retry-after"]).toBeDefined();
  });

  it("不同 IP 应独立计数", async () => {
    const agentA = request(app).set("X-Forwarded-For", "1.1.1.1");
    const agentB = request(app).set("X-Forwarded-For", "2.2.2.2");
    // IP-A 发送 100 次
    for (let i = 0; i < 100; i++) {
      await agentA.get("/api/trips");
    }
    // IP-B 发送 1 次
    const res = await agentB.get("/api/trips");
    expect(res.status).toBe(200); // IP-B 未被限流
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                                  | 操作步骤                                             | 预期结果                                                                           |
| -------------- | ----------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------- |
| INFRA-008-I-01 | 完整限流周期： burst → 限流 → 等待 → 恢复 | 1. 快速发送 200 次请求 2. 等待 61 秒 3. 再发送 10 次 | 步骤 1 中后 100 次返回 429；步骤 3 全部返回 200                                    |
| INFRA-008-I-02 | 限流触发时响应头包含正确信息              | 触发限流（第 101 次请求）                            | 响应头包含 `X-RateLimit-Limit: 100`、`X-RateLimit-Remaining: 0`、`Retry-After: 60` |

---

### 1.9 INFRA-009 城市知识库 JSON 准备

#### 功能验收测试用例

| 用例ID         | 用例描述               | 前置条件         | 操作步骤                           | 预期结果                                                        |
| -------------- | ---------------------- | ---------------- | ---------------------------------- | --------------------------------------------------------------- |
| INFRA-009-F-01 | 城市 JSON 文件格式正确 | —                | 读取 `knowledge/beijing.json`      | JSON 可解析，含 `cityId`/`cityName`/`pois`/`restaurants` 等字段 |
| INFRA-009-F-02 | POI 坐标格式正确       | 城市 JSON 已加载 | 检查 `pois[].latitude`/`longitude` | 坐标在合理范围（中国境内：纬度 18~54，经度 73~135）             |
| INFRA-009-F-03 | 必游景点标记正确       | 城市 JSON 已加载 | 检查 `pois[].isMustSee`            | 必游景点（如北京故宫）`isMustSee=true`                          |
| INFRA-009-F-04 | 多城市 JSON 可同时加载 | —                | 启动服务，请求生成多城市行程       | 所有城市的知识库均被正确加载                                    |

#### 边界验收测试用例

| 用例ID         | 用例描述                    | 前置条件                   | 操作步骤               | 预期结果                                           |
| -------------- | --------------------------- | -------------------------- | ---------------------- | -------------------------------------------------- |
| INFRA-009-B-01 | JSON 文件缺失时给出明确错误 | `beijing.json` 不存在      | 启动服务或请求北京行程 | 报错「城市知识库文件不存在：beijing.json」，不崩溃 |
| INFRA-009-B-02 | JSON 格式错误时给出明确错误 | `beijing.json` 含非法 JSON | 启动服务               | 报错「城市知识库格式错误：beijing.json」，指出行号 |
| INFRA-009-B-03 | POI 数量为 0 时引擎给出提示 | 城市 JSON 中 `pois=[]`     | 生成该城市行程         | 返回「该城市暂无景点数据，建议更换目的地」         |

#### 单元测试用例

```typescript
// packages/backend/tests/city-knowledge.test.ts

describe("城市知识库加载", () => {
  it("应正确加载北京知识库", () => {
    const beijing = CityKnowledgeFactory.load("beijing");
    expect(beijing.cityName).toBe("北京");
    expect(beijing.pois.length).toBeGreaterThan(0);
  });

  it("缺失城市时应抛出 CityKnowledgeNotFoundError", () => {
    expect(() => CityKnowledgeFactory.load("nonexistent")).toThrow(
      CityKnowledgeNotFoundError,
    );
  });

  it("POI 坐标应在合理范围内", () => {
    const beijing = CityKnowledgeFactory.load("beijing");
    beijing.pois.forEach((poi) => {
      expect(poi.latitude).toBeGreaterThan(18);
      expect(poi.latitude).toBeLessThan(54);
    });
  });
});
```

#### 集成测试用例

| 用例ID         | 用例描述                             | 操作步骤                                                   | 预期结果                                           |
| -------------- | ------------------------------------ | ---------------------------------------------------------- | -------------------------------------------------- |
| INFRA-009-I-01 | 生成行程时正确读取城市知识库         | 1. 准备 `beijing.json` 2. 调用生成接口，目的地=北京        | 返回行程中的 POI 均来自 `beijing.json`，无额外数据 |
| INFRA-009-I-02 | 城市知识库更新后无需重启服务即可生效 | 1. 修改 `beijing.json`（新增一个 POI） 2. 重新调用生成接口 | 新 POI 出现在生成结果中                            |

---

### 1.10 INFRA-010 CI/CD 基础流水线

#### 功能验收测试用例

| 用例ID         | 用例描述               | 前置条件                  | 操作步骤               | 预期结果                                    |
| -------------- | ---------------------- | ------------------------- | ---------------------- | ------------------------------------------- |
| INFRA-010-F-01 | 推送代码触发 CI        | GitHub 仓库已配置 Actions | 推送 commit 到 `main`  | GitHub Actions 自动运行 lint → test → build |
| INFRA-010-F-02 | 全部检查通过时 CI 通过 | 代码无 lint/test 错误     | 推送 commit            | Actions 显示绿色✅                          |
| INFRA-010-F-03 | Lint 错误时 CI 失败    | 代码含 ESLint 错误        | 推送 commit            | Actions 显示红色❌，日志指出 lint 错误位置  |
| INFRA-010-F-04 | 构建产物正确生成       | CI 通过                   | 查看 Actions Artifacts | `dist/` 目录被正确打包并可供下载            |

#### 边界验收测试用例

| 用例ID         | 用例描述                       | 前置条件                         | 操作步骤    | 预期结果                                   |
| -------------- | ------------------------------ | -------------------------------- | ----------- | ------------------------------------------ |
| INFRA-010-B-01 | CI 超时（>15 分钟）时自动取消  | 测试用例故意写无限循环           | 推送 commit | CI 在 15 分钟后自动取消，显示「Timed out」 |
| INFRA-010-B-02 | 并发推送时 CI 正确排队         | 同时推送 5 个 commit             | —           | CI 按推送顺序执行，不混乱                  |
| INFRA-010-B-03 | 依赖安装失败时 CI 给出明确错误 | 在 package.json 中加入不存在的包 | 推送 commit | CI 失败，日志显示 `npm ERR! 404 Not Found` |

#### 单元测试用例

```yaml
# .github/workflows/ci.yml（验收标准之一：文件存在且配置正确）

name: CI
on: [push, pull_request]
jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run lint
      - run: npm run test
      - run: npm run build
```

#### 集成测试用例

| 用例ID         | 用例描述                                    | 操作步骤                        | 预期结果                                                 |
| -------------- | ------------------------------------------- | ------------------------------- | -------------------------------------------------------- |
| INFRA-010-I-01 | 完整 CI 流程：push→lint→test→build→artifact | 推送一个无错误的 commit 到 main | 所有步骤通过，Artifact 可下载                            |
| INFRA-010-I-02 | PR 合并前 CI 必须通过                       | 提交一个未通过 CI 的 PR         | GitHub 禁止合并，显示「Some checks were not successful」 |

---

## 2. 阶段二：数据库层测试用例

---

### 2.1 DB-001 users 表

#### 功能验收测试用例

| 用例ID      | 用例描述                     | 前置条件   | 操作步骤                                                | 预期结果                                 |
| ----------- | ---------------------------- | ---------- | ------------------------------------------------------- | ---------------------------------------- |
| DB-001-F-01 | 创建用户成功                 | —          | `prisma.user.create({ email, passwordHash, nickname })` | 用户记录写入 DB，`createdAt` 自动生成    |
| DB-001-F-02 | 邮箱唯一性约束生效           | 用户已存在 | 再次创建同一邮箱的用户                                  | 抛出 `P2002`（Unique constraint failed） |
| DB-001-F-03 | 软删除用户（isDeleted=true） | 用户已存在 | `prisma.user.update({ isDeleted: true })`               | 用户记录仍存在，但查询时被过滤           |
| DB-001-F-04 | 更新昵称成功                 | 用户已存在 | `prisma.user.update({ nickname: '新昵称' })`            | DB 中 `nickname` 更新，`updatedAt` 更新  |

#### 边界验收测试用例

| 用例ID      | 用例描述                           | 前置条件 | 操作步骤                           | 预期结果                        |
| ----------- | ---------------------------------- | -------- | ---------------------------------- | ------------------------------- |
| DB-001-B-01 | 邮箱格式错误时被应用层拒绝         | —        | 创建用户，email=`not-an-email`     | 应用层校验失败，不发起 DB 写入  |
| DB-001-B-02 | 昵称超长（>50 字符）时被应用层拒绝 | —        | 创建用户，nickname=51 字符的字符串 | 应用层校验失败                  |
| DB-001-B-03 | 密码 Hash 为空时写入失败           | —        | 创建用户，`passwordHash=''`        | Prisma 抛出验证错误（字段必填） |

#### 单元测试用例

```typescript
// packages/backend/tests/db-users.test.ts

describe("users 表", () => {
  it("应拒绝重复邮箱", async () => {
    await prisma.user.create({
      data: { email: "dup@test.com", passwordHash: "h1", nickname: "u1" },
    });
    await expect(
      prisma.user.create({
        data: { email: "dup@test.com", passwordHash: "h2", nickname: "u2" },
      }),
    ).rejects.toThrow("Unique constraint");
  });

  it("软删除后默认查询不返回该用户", async () => {
    const user = await createTestUser();
    await prisma.user.update({
      where: { id: user.id },
      data: { isDeleted: true },
    });
    const found = await prisma.user.findMany({ where: { isDeleted: false } });
    expect(found.map((u) => u.id)).not.toContain(user.id);
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                           | 操作步骤                                                                                | 预期结果               |
| ----------- | ---------------------------------- | --------------------------------------------------------------------------------------- | ---------------------- |
| DB-001-I-01 | 注册→登录→修改昵称→软删除 完整流程 | 1. POST /auth/register 2. POST /auth/login 3. PUT /user/profile 4. DELETE /user/account | 每个步骤数据库状态正确 |

---

### 2.2 DB-002 trips 表

#### 功能验收测试用例

| 用例ID      | 用例描述                           | 前置条件         | 操作步骤                                       | 预期结果                                                 |
| ----------- | ---------------------------------- | ---------------- | ---------------------------------------------- | -------------------------------------------------------- |
| DB-002-F-01 | 创建行程成功                       | 用户已存在       | `prisma.trip.create({ userId, ... })`          | 行程记录写入 DB，`status=draft`                          |
| DB-002-F-02 | 外键约束生效（删除用户时级联处理） | 行程已存在       | 删除用户                                       | 根据设计决定：行程保留（`onDelete: Restrict`）或级联删除 |
| DB-002-F-03 | 按 userId 查询行程列表             | 用户有多个行程   | `prisma.trip.findMany({ where: { userId } })`  | 返回该用户所有行程，按 `createdAt` 倒序                  |
| DB-002-F-04 | 更新行程状态成功                   | 行程状态=`draft` | `prisma.trip.update({ status: 'generating' })` | DB 中 `status` 更新                                      |

#### 边界验收测试用例

| 用例ID      | 用例描述                             | 前置条件 | 操作步骤                       | 预期结果                             |
| ----------- | ------------------------------------ | -------- | ------------------------------ | ------------------------------------ |
| DB-002-B-01 | destinations JSON 格式错误时写入失败 | —        | 写入 `destinations='not-json'` | Prisma 校验失败（字段类型为 `Json`） |
| DB-002-B-02 | totalDays=0 时应用层拒绝             | —        | 创建行程 `totalDays=0`         | 应用层校验失败（总天数 ≥1）          |
| DB-002-B-03 | 用户不存在时创建行程失败             | —        | 使用不存在的 userId 创建行程   | Prisma 抛出外键错误（`P2003`）       |

#### 单元测试用例

```typescript
// packages/backend/tests/db-trips.test.ts

describe("trips 表", () => {
  it("应正确存储 destinations JSON", async () => {
    const trip = await createTestTrip({
      destinations: [{ cityId: "beijing", cityName: "北京", stayDays: 3 }],
    });
    const reloaded = await prisma.trip.findUnique({ where: { id: trip.id } });
    expect(reloaded.destinations[0].cityName).toBe("北京");
  });

  it("status 枚举值应受限", async () => {
    await expect(
      prisma.trip.create({
        data: { userId, status: "invalid_status" /* 其他必填字段 */ },
      }),
    ).rejects.toThrow();
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                          | 操作步骤                                                                                                           | 预期结果                      |
| ----------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------- |
| DB-002-I-01 | 创建行程→生成中→生成完成 状态流转 | 1. POST /trips/validate（创建 draft） 2. POST /trips/generate（status→generating） 3. 生成完成（status→completed） | 每次状态变更都正确持久化到 DB |

---

### 2.3 DB-003 day_plans 表

#### 功能验收测试用例

| 用例ID      | 用例描述                      | 前置条件        | 操作步骤                                                    | 预期结果                 |
| ----------- | ----------------------------- | --------------- | ----------------------------------------------------------- | ------------------------ |
| DB-003-F-01 | 创建天计划成功                | 行程已存在      | `prisma.dayPlan.create({ tripId, dayIndex, dayType })`      | 天计划写入 DB            |
| DB-003-F-02 | 按 tripId + dayIndex 唯一查询 | 天计划已存在    | `prisma.dayPlan.findUnique({ where: { tripId_dayIndex } })` | 返回正确记录             |
| DB-003-F-03 | timelineItems JSON 正确存储   | 天计划已存在    | 更新 `timelineItems=[{type:'poi',...}]`                     | 重新读取后 JSON 结构完整 |
| DB-003-F-04 | 删除行程时天计划级联删除      | 行程有 3 天计划 | 删除行程                                                    | 3 条天计划同时被删除     |

#### 边界验收测试用例

| 用例ID      | 用例描述                                 | 前置条件         | 操作步骤                       | 预期结果               |
| ----------- | ---------------------------------------- | ---------------- | ------------------------------ | ---------------------- |
| DB-003-B-01 | dayIndex 超出 totalDays 时应用层拒绝     | 行程 totalDays=5 | 创建 dayIndex=5（第6天）       | 应用层校验失败         |
| DB-003-B-02 | timelineItems 为空时仍有效               | —                | 创建天计划，`timelineItems=[]` | 写入成功（允许空日程） |
| DB-003-B-03 | timelineItems 单个 item 超过 10KB 时拒绝 | —                | 写入超大 item（>10KB）         | 应用层校验失败         |

#### 单元测试用例

```typescript
// packages/backend/tests/db-dayplans.test.ts

describe("day_plans 表", () => {
  it("同一 tripId+dayIndex 组合应唯一", async () => {
    await prisma.dayPlan.create({ tripId, dayIndex: 0, dayType: "explore" });
    await expect(
      prisma.dayPlan.create({
        tripId,
        dayIndex: 0,
        dayType: "explore",
      }),
    ).rejects.toThrow("Unique constraint");
  });

  it("dayType 应为合法枚举值", async () => {
    await expect(
      prisma.dayPlan.create({
        tripId,
        dayIndex: 0,
        dayType: "invalid_type",
      }),
    ).rejects.toThrow();
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                        | 操作步骤     | 预期结果                                                                      |
| ----------- | ------------------------------- | ------------ | ----------------------------------------------------------------------------- |
| DB-003-I-01 | 生成完成后 day_plans 表数据完整 | 完成行程生成 | 每个 day_plan 的 `timelineItems` 非空，`dayType` 正确（move/explore/transit） |

---

### 2.4 DB-004 timeline_items 表（方案：存 JSON vs 独立表）

> 注：根据数据库设计规格书，timeline_items 存储为 day_plans 表内的 JSON 字段，不单独建表。此处测试用例针对 JSON 字段内容的结构校验。

#### 功能验收测试用例

| 用例ID      | 用例描述                      | 前置条件               | 操作步骤                                                   | 预期结果                    |
| ----------- | ----------------------------- | ---------------------- | ---------------------------------------------------------- | --------------------------- |
| DB-004-F-01 | timelineItems JSON 含必需字段 | day_plan 已创建        | 写入 `timelineItems=[{id, type, startTime, endTime, ...}]` | 重新读取后字段完整          |
| DB-004-F-02 | alternatives JSON 正确存储    | day_plan 已创建        | 写入含 alternatives 的 item                                | alternatives 数组正确持久化 |
| DB-004-F-03 | 跨天 split 标记正确存储       | 中转日 day_plan 已创建 | 写入 `isCrossDaySplit=true` 的 item                        | 标记正确保存                |

#### 边界验收测试用例

| 用例ID      | 用例描述                             | 前置条件     | 操作步骤                              | 预期结果                             |
| ----------- | ------------------------------------ | ------------ | ------------------------------------- | ------------------------------------ |
| DB-004-B-01 | timelineItems 总时间超过当天可用时间 | 当天可用 12h | 写入累计 13h 的 items                 | 应用层警告（不阻止保存，但前端提示） |
| DB-004-B-02 | JSON 深度嵌套（>5 层）时拒绝         | —            | 写入超深嵌套 JSON                     | 应用层校验失败                       |
| DB-004-B-03 | startTime > endTime 时应用层拒绝     | —            | 写入 `startTime=18:00, endTime=10:00` | 应用层校验失败                       |

#### 单元测试用例

```typescript
// packages/backend/tests/db-timeline-items.test.ts

describe("timeline_items JSON 结构校验", () => {
  it("poi 类型 item 应含 requiredPoiFields", () => {
    const item = { id: "1", type: "poi", startTime: "09:00", endTime: "11:00" };
    // 缺少 poiName, latitude, longitude
    expect(validateTimelineItem(item)).toBe(false);
  });

  it("时间逻辑应合理", () => {
    const item = { startTime: "09:00", endTime: "11:00" };
    expect(validateTimeRange(item.startTime, item.endTime)).toBe(true);
    expect(validateTimeRange("18:00", "10:00")).toBe(false); // 跨天需标记 isCrossDaySplit
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                                  | 操作步骤                                                                  | 预期结果                            |
| ----------- | ----------------------------------------- | ------------------------------------------------------------------------- | ----------------------------------- |
| DB-004-I-01 | 端到端：生成→读取→编辑→保存 timelineItems | 1. 生成行程 2. GET /trips/{tripId}/day/{dayIndex} 3. PUT 编辑 4. 重新 GET | 每次读取的 timelineItems 与写入一致 |

---

### 2.5 DB-005 hotel_recommendations 表

#### 功能验收测试用例

| 用例ID      | 用例描述                         | 前置条件            | 操作步骤                                                        | 预期结果             |
| ----------- | -------------------------------- | ------------------- | --------------------------------------------------------------- | -------------------- |
| DB-005-F-01 | 创建酒店推荐成功                 | day_plan 已存在     | `prisma.hotelRecommendation.create({ dayPlanId, ... })`         | 记录写入 DB          |
| DB-005-F-02 | 按 dayPlanId 查询酒店推荐        | 酒店推荐已存在      | `prisma.hotelRecommendation.findMany({ where: { dayPlanId } })` | 返回该天所有酒店推荐 |
| DB-005-F-03 | priceRange 字段正确存储          | —                   | 写入 `priceRange='300-500'`                                     | 重新读取后值正确     |
| DB-005-F-04 | 删除 day_plan 时酒店推荐级联删除 | day_plan 有酒店推荐 | 删除 day_plan                                                   | 酒店推荐同时被删除   |

#### 边界验收测试用例

| 用例ID      | 用例描述                                    | 前置条件 | 操作步骤                        | 预期结果                         |
| ----------- | ------------------------------------------- | -------- | ------------------------------- | -------------------------------- |
| DB-005-B-01 | facilities 数组超过 50 项时拒绝             | —        | 写入 51 个设施                  | 应用层校验失败（合理上限）       |
| DB-005-B-02 | 酒店推荐无价格信息（priceRange=null）时有效 | —        | 创建酒店推荐，`priceRange=null` | 写入成功（价格可空缺）           |
| DB-005-B-03 | 同一 dayPlanId 创建超过 10 条推荐时警告     | —        | 创建 11 条推荐                  | 应用层警告（不阻止，但记录日志） |

#### 单元测试用例

```typescript
// packages/backend/tests/db-hotels.test.ts

describe("hotel_recommendations 表", () => {
  it("应关联到正确的 day_plan", async () => {
    const hotel = await createTestHotelRecommendation();
    const dayPlan = await prisma.dayPlan.findUnique({
      where: { id: hotel.dayPlanId },
    });
    expect(dayPlan).not.toBeNull();
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                   | 操作步骤       | 预期结果                                                         |
| ----------- | -------------------------- | -------------- | ---------------------------------------------------------------- |
| DB-005-I-01 | 生成行程后酒店推荐正确入库 | 生成多城市行程 | 每个城市第一天对应的 day_plan 有关联的 hotel_recommendation 记录 |

---

### 2.6 DB-006 cities 表 + 首批数据

#### 功能验收测试用例

| 用例ID      | 用例描述                | 前置条件       | 操作步骤                                                   | 预期结果                     |
| ----------- | ----------------------- | -------------- | ---------------------------------------------------------- | ---------------------------- |
| DB-006-F-01 | cities 表 seed 数据成功 | 数据库已初始化 | `npx prisma db seed`                                       | 至少 50 个城市写入 cities 表 |
| DB-006-F-02 | 按 cityId 查询城市      | 数据已 seed    | `prisma.city.findUnique({ where: { cityId: 'beijing' } })` | 返回北京市完整记录           |
| DB-006-F-03 | 按首字母查询城市列表    | 数据已 seed    | `prisma.city.findMany({ where: { firstLetter: 'B' } })`    | 返回所有 B 开头城市          |
| DB-006-F-04 | 热门城市标记正确        | 数据已 seed    | `prisma.city.findMany({ where: { isPopular: true } })`     | 返回 SRS 定义的热门城市      |

#### 边界验收测试用例

| 用例ID      | 用例描述                     | 前置条件                  | 操作步骤  | 预期结果                        |
| ----------- | ---------------------------- | ------------------------- | --------- | ------------------------------- |
| DB-006-B-01 | cityId 重复时 seed 失败      | seed 脚本含重复 cityId    | 运行 seed | 报错并停止，已有数据回滚        |
| DB-006-B-02 | 坐标不在合理范围时 seed 警告 | seed 数据含错误坐标       | 运行 seed | 警告日志，但继续（不阻止 seed） |
| DB-006-B-03 | 城市名为空时写入失败         | seed 数据含 `cityName=''` | 运行 seed | Prisma 验证错误                 |

#### 单元测试用例

```typescript
// packages/backend/tests/db-cities.test.ts

describe("cities 表", () => {
  it("应正确识别热门城市", async () => {
    const popular = await prisma.city.findMany({ where: { isPopular: true } });
    expect(popular.length).toBeGreaterThan(0);
    expect(popular.map((c) => c.cityId)).toContain("beijing");
  });

  it("cityId 应唯一", async () => {
    await prisma.city.create({
      data: { cityId: "test-city", cityName: "测试" },
    });
    await expect(
      prisma.city.create({
        data: { cityId: "test-city", cityName: "重复" },
      }),
    ).rejects.toThrow("Unique constraint");
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                   | 操作步骤                      | 预期结果                                                   |
| ----------- | -------------------------- | ----------------------------- | ---------------------------------------------------------- |
| DB-006-I-01 | 前端城市选择器加载热门城市 | 1. 启动前端 2. 点击城市选择器 | 热门城市列表正确显示，与 DB 中 `isPopular=true` 的记录一致 |

---

### 2.7 DB-007 generation_tasks 表（P1）

#### 功能验收测试用例

| 用例ID      | 用例描述                   | 前置条件        | 操作步骤                                                                  | 预期结果                   |
| ----------- | -------------------------- | --------------- | ------------------------------------------------------------------------- | -------------------------- |
| DB-007-F-01 | 创建生成任务记录           | 行程已存在      | `prisma.generationTask.create({ tripId, status: 'pending' })`             | 记录写入 DB                |
| DB-007-F-02 | 更新任务状态为 processing  | 任务已创建      | `prisma.generationTask.update({ status: 'processing' })`                  | DB 中 `startedAt` 自动更新 |
| DB-007-F-03 | 任务完成后更新 completedAt | 任务 processing | `prisma.generationTask.update({ status: 'completed' })`                   | `completedAt` 自动写入     |
| DB-007-F-04 | 任务失败记录错误信息       | 任务 processing | `prisma.generationTask.update({ status: 'failed', errorMessage: '...' })` | 错误信息正确保存           |

#### 边界验收测试用例

| 用例ID      | 用例描述                               | 前置条件                   | 操作步骤                            | 预期结果                                     |
| ----------- | -------------------------------------- | -------------------------- | ----------------------------------- | -------------------------------------------- |
| DB-007-B-01 | 同一 tripId 同时只能有一个 active 任务 | 已有 status=pending 的任务 | 创建第二个任务                      | 应用层拒绝，返回「该行程已有生成任务进行中」 |
| DB-007-B-02 | errorMessage 超过 2000 字符时截断      | —                          | 写入超长 errorMessage               | 截断到 2000 字符，记录日志                   |
| DB-007-B-03 | 任务超时（>30 分钟）自动标记为 failed  | —                          | 任务 status=processing 超过 30 分钟 | 定时任务扫描并标记为 failed                  |

#### 单元测试用例

```typescript
// packages/backend/tests/db-generation-tasks.test.ts

describe("generation_tasks 表", () => {
  it("同一 tripId 不应有两个 active 任务", async () => {
    await prisma.generationTask.create({ tripId, status: "pending" });
    const active = await prisma.generationTask.findMany({
      where: { tripId, status: { in: ["pending", "processing"] } },
    });
    expect(active.length).toBe(1);
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                         | 操作步骤                                                               | 预期结果                                  |
| ----------- | -------------------------------- | ---------------------------------------------------------------------- | ----------------------------------------- |
| DB-007-I-01 | 生成任务从创建到完成的全生命周期 | 1. POST /trips/generate 2. 轮询 GET /trips/generate/status 3. 生成完成 | generation_tasks 表正确记录各状态及时间戳 |

---

### 2.8 DB-008 trip_share_links 表（P1）

#### 功能验收测试用例

| 用例ID      | 用例描述                        | 前置条件       | 操作步骤                                                     | 预期结果                          |
| ----------- | ------------------------------- | -------------- | ------------------------------------------------------------ | --------------------------------- |
| DB-008-F-01 | 创建分享链接成功                | 行程已存在     | `prisma.tripShareLink.create({ tripId, shareToken, ... })`   | 记录写入 DB                       |
| DB-008-F-02 | 通过 shareToken 查询行程        | 分享链接已创建 | `prisma.tripShareLink.findUnique({ where: { shareToken } })` | 返回关联行程                      |
| DB-008-F-03 | 访问次数（accessCount）自动递增 | 分享链接已创建 | 每次查询后 `accessCount + 1`                                 | DB 中计数正确                     |
| DB-008-F-04 | 过期分享链接查询返回 null       | 分享链接已过期 | 查询过期链接                                                 | 返回 null，前端提示「分享已过期」 |

#### 边界验收测试用例

| 用例ID      | 用例描述                     | 前置条件 | 操作步骤                    | 预期结果                   |
| ----------- | ---------------------------- | -------- | --------------------------- | -------------------------- |
| DB-008-B-01 | shareToken 碰撞时重新生成    | —        | Mock 生成碰撞 token         | 系统自动重新生成，直到唯一 |
| DB-008-B-02 | expireAt 为 null 时永不过期  | —        | 创建 `expireAt=null` 的分享 | 查询时不过期判断           |
| DB-008-B-03 | 访问次数超过 1000 时仍可访问 | —        | 模拟访问 1001 次            | 仍可访问（无访问次数上限） |

#### 单元测试用例

```typescript
// packages/backend/tests/db-share-links.test.ts

describe("trip_share_links 表", () => {
  it("shareToken 应唯一", async () => {
    const token = generateShareToken();
    await prisma.tripShareLink.create({ tripId, shareToken: token });
    await expect(
      prisma.tripShareLink.create({
        tripId,
        shareToken: token,
      }),
    ).rejects.toThrow("Unique constraint");
  });

  it("过期链接应返回 null", async () => {
    const expired = await createExpiredShareLink();
    const found = await prisma.tripShareLink.findFirst({
      where: {
        shareToken: expired.shareToken,
        OR: [{ expireAt: null }, { expireAt: { gt: new Date() } }],
      },
    });
    expect(found).toBeNull();
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                | 操作步骤                                                                    | 预期结果                                |
| ----------- | ----------------------- | --------------------------------------------------------------------------- | --------------------------------------- |
| DB-008-I-01 | 分享→访问→查看 完整流程 | 1. POST /trips/{tripId}/share 2. 使用 shareToken 访问 3. GET /share/{token} | 每次访问 accessCount 递增，返回正确行程 |

---

### 2.9 DB-009 llm_call_logs 表（P1）

#### 功能验收测试用例

| 用例ID      | 用例描述                    | 前置条件       | 操作步骤                                            | 预期结果                                  |
| ----------- | --------------------------- | -------------- | --------------------------------------------------- | ----------------------------------------- |
| DB-009-F-01 | LLM 调用日志记录成功        | —              | 每次 LLM 调用后 `prisma.llmCallLog.create(...)`     | 记录写入 DB，含 provider/model/tokenUsage |
| DB-009-F-02 | 按 tripId 查询 LLM 调用记录 | 有调用记录     | `prisma.llmCallLog.findMany({ where: { tripId } })` | 返回该行程所有 LLM 调用                   |
| DB-009-F-03 | 失败调用记录 errorMessage   | LLM 调用失败   | 写入 `status='failed', errorMessage='...'`          | 错误信息正确保存                          |
| DB-009-F-04 | 日志超过 90 天自动归档      | 有 90 天前日志 | 运行归档脚本                                        | 旧日志移至 `llm_call_logs_archive` 或删除 |

#### 边界验收测试用例

| 用例ID      | 用例描述                        | 前置条件               | 操作步骤        | 预期结果                                 |
| ----------- | ------------------------------- | ---------------------- | --------------- | ---------------------------------------- |
| DB-009-B-01 | prompt 超长（>100KB）时截断存储 | —                      | 写入超大 prompt | 截断到 100KB，记录警告日志               |
| DB-009-B-02 | DB 写入失败时不影响主流程       | llm_call_logs 表被锁   | LLM 调用        | LLM 调用正常返回，日志写入失败仅记录警告 |
| DB-009-B-03 | 短时间内大量日志写入时排队      | 1 秒内 100 次 LLM 调用 | —               | 日志批量写入，不阻塞主流程               |

#### 单元测试用例

```typescript
// packages/backend/tests/db-llm-logs.test.ts

describe("llm_call_logs 表", () => {
  it("应正确记录 token 使用量", async () => {
    const log = await createTestLLMLog({
      tokenUsage: { input: 100, output: 200 },
    });
    expect(log.tokenUsage.input).toBe(100);
    expect(log.tokenUsage.output).toBe(200);
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                        | 操作步骤      | 预期结果                                           |
| ----------- | ------------------------------- | ------------- | -------------------------------------------------- |
| DB-009-I-01 | 完整生成流程的 LLM 调用均有日志 | 生成 5 天行程 | llm_call_logs 表有 5 条以上记录，每条含 tokenUsage |

---

### 2.10 DB-010 核心索引

#### 功能验收测试用例

| 用例ID      | 用例描述                                  | 前置条件          | 操作步骤                                     | 预期结果                          |
| ----------- | ----------------------------------------- | ----------------- | -------------------------------------------- | --------------------------------- |
| DB-010-F-01 | trips(userId) 索引加速查询                | 用户有 100 个行程 | `SELECT * FROM trips WHERE userId=...`       | 查询计划显示使用索引，耗时 < 50ms |
| DB-010-F-02 | day_plans(tripId, dayIndex) 唯一索引      | —                 | 尝试插入重复 key                             | DB 拒绝，抛出唯一约束错误         |
| DB-010-F-03 | cities(firstLetter) 索引加速首字母查询    | 50+ 城市          | `SELECT * FROM cities WHERE firstLetter='B'` | 使用索引，耗时 < 20ms             |
| DB-010-F-04 | llm_call_logs(tripId, createdAt) 复合索引 | 有日志数据        | 按 tripId + 时间范围查询                     | 使用复合索引，耗时 < 100ms        |

#### 边界验收测试用例

| 用例ID      | 用例描述                           | 前置条件                | 操作步骤               | 预期结果                                     |
| ----------- | ---------------------------------- | ----------------------- | ---------------------- | -------------------------------------------- |
| DB-010-B-01 | 索引缺失时查询警告                 | 删除 trips(userId) 索引 | 执行查询               | 日志输出「Slow query warning」，但不影响结果 |
| DB-010-B-02 | 同时创建多个索引时事务正确         | —                       | 运行迁移创建 10 个索引 | 全部成功或全部回滚                           |
| DB-010-B-03 | 索引建立中（CONCURRENTLY）时可读写 | 大表建索引              | 建索引过程中执行读写   | 读写正常，不阻塞                             |

#### 单元测试用例

```sql
-- 索引存在性验证（迁移后执行）

SELECT indexname, tablename
FROM pg_indexes
WHERE schemaname = 'public'
AND tablename IN ('trips', 'day_plans', 'cities', 'llm_call_logs');
-- 预期：返回所有核心索引
```

#### 集成测试用例

| 用例ID      | 用例描述                                        | 操作步骤                                             | 预期结果                                       |
| ----------- | ----------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------- |
| DB-010-I-01 | 数据量增长后索引仍有效（1000 行程、10000 天级） | 1. 批量插入 1000 个行程 2. 每个行程 5 天 3. 执行查询 | 查询耗时无明显增长（与 10 条数据时对比 < 2倍） |

---

### 2.11 DB-011 软删除触发器（P1）

#### 功能验收测试用例

| 用例ID      | 用例描述                   | 前置条件     | 操作步骤                          | 预期结果                                                          |
| ----------- | -------------------------- | ------------ | --------------------------------- | ----------------------------------------------------------------- |
| DB-011-F-01 | 软删除用户后相关行程不可见 | 用户有行程   | `UPDATE users SET isDeleted=true` | `SELECT * FROM trips WHERE userId=... AND isDeleted=false` 返回空 |
| DB-011-F-02 | 软删除不实际删除数据       | 用户被软删除 | 直接查询 DB                       | 用户记录和行程记录仍存在                                          |
| DB-011-F-03 | 管理员可查看软删除的数据   | 管理员权限   | 查询时跳过 `isDeleted` 过滤       | 返回所有记录（含软删除）                                          |

#### 边界验收测试用例

| 用例ID      | 用例描述                            | 前置条件       | 操作步骤                  | 预期结果                                       |
| ----------- | ----------------------------------- | -------------- | ------------------------- | ---------------------------------------------- |
| DB-011-B-01 | 重复软删除（已经是 isDeleted=true） | —              | 再次 `SET isDeleted=true` | 无错误，幂等                                   |
| DB-011-B-02 | 软删除后重新注册同一邮箱            | 用户已被软删除 | 使用同一邮箱注册          | 允许（软删除后邮箱可重用）或拒绝（按产品设计） |

#### 单元测试用例

```typescript
// packages/backend/tests/db-soft-delete.test.ts

describe("软删除触发器", () => {
  it("软删除用户后默认查询不返回其行程", async () => {
    const user = await createTestUser();
    await createTestTrip({ userId: user.id });
    await prisma.user.update({
      where: { id: user.id },
      data: { isDeleted: true },
    });
    const trips = await prisma.trip.findMany({
      where: { userId: user.id, isDeleted: false },
    });
    expect(trips.length).toBe(0);
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                 | 操作步骤                                  | 预期结果                         |
| ----------- | ------------------------ | ----------------------------------------- | -------------------------------- |
| DB-011-I-01 | 软删除用户→尝试登录→失败 | 1. 注册用户 2. 软删除 3. 使用同一邮箱登录 | 登录失败，「用户不存在或已注销」 |

---

### 2.12 DB-012 知识库工厂模式

#### 功能验收测试用例

| 用例ID      | 用例描述                     | 前置条件                      | 操作步骤                                       | 预期结果                                        |
| ----------- | ---------------------------- | ----------------------------- | ---------------------------------------------- | ----------------------------------------------- |
| DB-012-F-01 | 工厂正确加载城市 JSON        | `knowledge/beijing.json` 存在 | `CityKnowledgeFactory.load('beijing')`         | 返回 `CityKnowledge` 对象                       |
| DB-012-F-02 | 工厂按城市类型加载不同知识库 | 平原城市/山海城市 JSON 均存在 | 分别加载 `beijing`（平原）和 `qingdao`（山海） | 两者均正确加载，城市类型字段不同                |
| DB-012-F-03 | 工厂缓存已加载的城市知识库   | 北京已加载                    | 再次 `load('beijing')`                         | 返回缓存对象，不重新读取文件                    |
| DB-012-F-04 | 未支持城市返回清晰错误       | —                             | `load('unsupported-city')`                     | 抛出 `UnsupportedCityError`，列出已支持城市列表 |

#### 边界验收测试用例

| 用例ID      | 用例描述                     | 前置条件                                       | 操作步骤                 | 预期结果                                 |
| ----------- | ---------------------------- | ---------------------------------------------- | ------------------------ | ---------------------------------------- |
| DB-012-B-01 | 城市 JSON 文件正在写入时读取 | 另一个进程正在写 `beijing.json`                | `load('beijing')`        | 等待文件写入完成或抛出「文件被占用」错误 |
| DB-012-B-02 | 知识库版本号不一致时给出警告 | JSON 中 `version=0.9`，代码期望 `version>=1.0` | `load('beijing')`        | 输出警告日志，但仍加载（向前兼容）       |
| DB-012-B-03 | 同时加载 100 个城市知识库    | —                                              | 并发调用 `load()` 100 次 | 内存占用 < 500MB，加载成功               |

#### 单元测试用例

```typescript
// packages/backend/tests/knowledge-factory.test.ts

describe("知识库工厂模式", () => {
  it("应缓存已加载的城市知识库", () => {
    const bj1 = CityKnowledgeFactory.load("beijing");
    const bj2 = CityKnowledgeFactory.load("beijing");
    expect(bj1).toBe(bj2); // 同一引用（缓存）
  });

  it("应拒绝加载不支持的城市", () => {
    expect(() => CityKnowledgeFactory.load("unknown-city")).toThrow(
      "City knowledge not found",
    );
  });
});
```

#### 集成测试用例

| 用例ID      | 用例描述                             | 操作步骤                                               | 预期结果                                              |
| ----------- | ------------------------------------ | ------------------------------------------------------ | ----------------------------------------------------- |
| DB-012-I-01 | 生成行程时引擎通过工厂正确获取知识库 | 1. 准备 `beijing.json` 2. 生成北京行程 3. 检查引擎日志 | 日志显示「Loaded knowledge for beijing from factory」 |

---

## 3. 阶段三：后端 API 层测试用例

---

### 3.1 API-001 POST /trips/validate

#### 功能验收测试用例

| 用例ID       | 用例描述                      | 前置条件 | 操作步骤                                                           | 预期结果                                                                             |
| ------------ | ----------------------------- | -------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| API-001-F-01 | 合法输入返回验证通过          | —        | POST `/trips/validate`，合法 Body                                  | 返回 `200`，`{ valid: true }`                                                        |
| API-001-F-02 | 缺失必填字段返回 400          | —        | POST `/trips/validate`，Body 不含 `destinations`                   | 返回 `400`，`{ error: 'MissingRequiredField', field: 'destinations' }`               |
| API-001-F-03 | 出发城市=目的地时返回冲突警告 | —        | `departureCity='beijing'`, `destinations=[{cityId:'beijing',...}]` | 返回 `200`，`{ valid: true, warnings: [{ type: 'departure_same_as_destination' }] }` |
| API-001-F-04 | 总天数超过 14 天返回错误      | —        | `destinations` 累计 stayDays > 14                                  | 返回 `400`，`{ error: 'TotalDaysExceedLimit', max: 14 }`                             |

#### 边界验收测试用例

| 用例ID       | 用例描述               | 前置条件 | 操作步骤                                                | 预期结果                                                 |
| ------------ | ---------------------- | -------- | ------------------------------------------------------- | -------------------------------------------------------- |
| API-001-B-01 | destinations 为空数组  | —        | `destinations=[]`                                       | 返回 `400`，`{ error: 'AtLeastOneDestinationRequired' }` |
| API-001-B-02 | 同一城市出现两次       | —        | `destinations=[{cityId:'beijing'}, {cityId:'beijing'}]` | 返回 `400`，`{ error: 'DuplicateDestination' }`          |
| API-001-B-03 | 儿童 >0 但年龄选择为空 | —        | `childrenCount=1`, `childrenAges=[]`                    | 返回 `400`，`{ error: 'ChildrenAgesRequired' }`          |

#### 单元测试用例

```typescript
// packages/backend/tests/api-validate.test.ts

describe('POST /trips/validate', () => {
  it('应拒绝总天数超过 14 天', async () => {
    const res = await request(app)
      .post('/api/trips/validate')
      .send({ destinations: [{ cityId:'bj', stayDays:15 }], ... });
    expect(res.status).toBe(400);
    expect(res.body.error).toBe('TotalDaysExceedLimit');
  });

  it('出发城市与目的地相同时返回警告（非错误）', async () => {
    const res = await request(app)
      .post('/api/trips/validate')
      .send({ departureCity: 'beijing', destinations: [{ cityId:'beijing', stayDays:2 }] });
    expect(res.status).toBe(200);
    expect(res.body.warnings).toContainEqual(expect.objectContaining({ type: 'departure_same_as_destination' }));
  });
});
```

#### 集成测试用例

| 用例ID       | 用例描述                              | 操作步骤                                                          | 预期结果           |
| ------------ | ------------------------------------- | ----------------------------------------------------------------- | ------------------ |
| API-001-I-01 | 前端表单提交→验证通过→创建 draft 行程 | 1. 前端填写表单 2. 点击「生成」触发验证 3. 验证通过后 POST /trips | draft 行程创建成功 |

---

### 3.2 API-002 POST /trips/generate（核心接口）

#### 功能验收测试用例

| 用例ID       | 用例描述                                   | 前置条件        | 操作步骤                                                | 预期结果                                                            |
| ------------ | ------------------------------------------ | --------------- | ------------------------------------------------------- | ------------------------------------------------------------------- |
| API-002-F-01 | 合法请求触发生成，返回 SSE 流              | 已通过 validate | POST `/trips/generate`，Header 接受 `text/event-stream` | 返回 `200`，Content-Type=`text/event-stream`，收到 `connected` 事件 |
| API-002-F-02 | 生成完成后收到 done 事件                   | 生成进行中      | 等待生成完成                                            | 收到 `done` 事件，含 `tripId` 和 `totalDays`                        |
| API-002-F-03 | 生成过程中收到 day_ready 事件              | 生成进行中      | 监听 SSE 事件                                           | 每个天完成后收到 `day_ready`，含 `dayIndex` 和 `dayPlan` 摘要       |
| API-002-F-04 | 未通过 validate 直接调用 generate 返回 400 | —               | 跳过 validate，直接 POST `/trips/generate`              | 返回 `400`，`{ error: 'ValidationRequired' }`                       |

#### 边界验收测试用例

| 用例ID       | 用例描述                           | 前置条件           | 操作步骤                                 | 预期结果                                                         |
| ------------ | ---------------------------------- | ------------------ | ---------------------------------------- | ---------------------------------------------------------------- |
| API-002-B-01 | 同一 tripId 同时只能有一个生成任务 | 已有生成任务进行中 | 对同一 tripId 再次调用 `/trips/generate` | 返回 `409`，`{ error: 'GenerationAlreadyInProgress' }`           |
| API-002-B-02 | 生成过程中客户端断开               | SSE 连接已建立     | 前端 `EventSource.close()`               | 后端取消正在进行的 LLM 调用，generation_tasks 标记为 `cancelled` |
| API-002-B-03 | 生成超过 5 分钟时 SSE 发送保活事件 | —                  | 某天生成超过 5 分钟                      | 每 30 秒收到 `heartbeat` 事件                                    |

#### 单元测试用例

```typescript
// packages/backend/tests/api-generate.test.ts

describe("POST /trips/generate", () => {
  it("应返回 SSE 流", async () => {
    const res = await request(app)
      .post("/api/trips/generate")
      .set("Accept", "text/event-stream")
      .send(validGenerateRequest);
    expect(res.headers["content-type"]).toContain("text/event-stream");
  });

  it("同一行程不应同时有两个生成任务", async () => {
    await request(app).post("/api/trips/generate").send(req1);
    const res2 = await request(app).post("/api/trips/generate").send(req1);
    expect(res2.status).toBe(409);
  });
});
```

#### 集成测试用例

| 用例ID       | 用例描述                                  | 操作步骤                                                                                   | 预期结果                                             |
| ------------ | ----------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------ |
| API-002-I-01 | 端到端：validate→generate→SSE 接收→结果页 | 1. POST /trips/validate 2. POST /trips/generate 3. 前端监听 SSE 4. 收到 done 5. 跳转结果页 | 每个步骤正确衔接，最终看到完整行程                   |
| API-002-I-02 | 生成中途失败时的降级处理                  | 在第 3 天生成时断开 LLM                                                                    | 收到 `error` 事件，含 `partialDays`（前 2 天已完成） | 前端显示「部分生成成功」，可查看已完成天数 |

---
